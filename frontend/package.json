{
  "name": "base-org-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node -e \"require('fs').rmSync('node_modules/.vite',{recursive:true,force:true})\" && vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "lint": "eslint . --ext .js"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "eslint": "^8.57.1",
    "vite": "^5.4.19",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "@toast-ui/editor": "^3.2.2",
    "dompurify": "^3.3.3",
    "marked": "^17.0.4",
    "turndown": "^7.2.2",
    "turndown-plugin-gfm": "^1.0.2"
  }
}
